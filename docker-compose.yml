services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
      args:
        - VITE_SUPABASE_URL=${VITE_SUPABASE_URL}
        - VITE_SUPABASE_ANON_KEY=${VITE_SUPABASE_ANON_KEY}
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always

  backend:
    build:
      context: ./whatsapp-server
    environment:
      - NODE_ENV=production
    volumes:
      - ./whatsapp-server/auth_info_baileys:/app/auth_info_baileys
    restart: always
